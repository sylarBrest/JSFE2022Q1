export type BikeData = {
  buy: boolean,
  category: string,
  color: string,
  manufacturer: string,
  name: string,
  photo: string,
  popular: boolean,
  size: string,
  stock: number,
  wheels: number,
  year: number
};

export type BikeDataArray = BikeData[];