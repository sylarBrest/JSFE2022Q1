export type BikeData = {
  num: number,
  buy: boolean,
  category: string,
  color: string,
  manufacturer: string,
  name: string,
  photo: string,
  popular: boolean,
  size: string,
  stock: number,
  wheels: number,
  year: number
};

export type BikeDataArray = BikeData[];
